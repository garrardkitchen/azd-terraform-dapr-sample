# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: dapr-test
services:  
  app:
    language: dotnet
    project: ./dapr-test.AppHost/dapr-test.AppHost.csproj
    host: containerapp
infra:
  provider: terraform
  path: infra
